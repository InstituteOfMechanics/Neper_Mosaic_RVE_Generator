[build-system]
requires = ["setuptools", "setuptools-scm"]
build-backend = "setuptools.build_meta"

[project]
name = "neper_mosaic"
authors = [
    {name = "Dilek Güzel", email = "dilek.guezel@tu-dortmund.de"},
    {name = "Tim Furlan", email = "tim.furlan@tu-dortmund.de"},
    {name = "Andreas Menzel", email = "andreas.menzel@tu-dortmund.de"}
]
description = "A tool to generate representative volume elements on rectilinear domains, based on microstructures generated by Neper."
version = "1.0.0"
readme = "README.md"
requires-python = ">=3.7"
dependencies = [
    "numpy",
    "gmsh>4.12",
    "pytest"
]
